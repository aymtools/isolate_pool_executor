name: isolate_pool_executor
description: isolate pool executor for Dart.
version: 1.1.4
homepage: https://github.com/aymtools/isolate_pool_executor
topics:
  - isolate
  - isolate-pool
  - pool

environment:
  sdk: '>=2.15.0 <4.0.0'