name: isolate_pool_executor
description: The Isolate Pool Executor is a tool in Dart for managing concurrent tasks by limiting and reusing isolates to optimize performance.
version: 1.2.2
homepage: https://github.com/aymtools/isolate_pool_executor
topics:
  - isolate
  - isolate-pool
  - pool

environment:
  sdk: '>=2.15.0 <4.0.0'